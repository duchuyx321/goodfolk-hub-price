query CartInput {
  cart {
    lines {
      id
      quantity
      sku: attribute(key: "_sku") {
        value
      }
      price: attribute(key: "_price") {
        value
      }
      cost {
        subtotalAmount {
          amount
        }
      }
    }
  }
  discount {
    discountClasses
  }
}
