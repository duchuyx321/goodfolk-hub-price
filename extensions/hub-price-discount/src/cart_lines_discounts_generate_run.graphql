query CartInput {
  cart {
    lines {
      id
      quantity
      sku: attribute(key: "_sku") {
        value
      }
      cost {
        subtotalAmount {
          amount
        }
      }
    }
  }
  discount {
    discountClasses
  }
  fetchResult {
    jsonBody
  }
}
